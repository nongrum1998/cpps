/**
 * @pension/hooks — shared React hooks for the workspace.
 *
 * Skeleton package. Intended to hold cross-app hooks (data fetching,
 * device capabilities, form helpers) shared between apps. Exports are
 * minimal until hooks are extracted from the app.
 */

/** Canonical npm package name for the hooks package. */
export const HOOKS_PACKAGE_NAME = '@pension/hooks';
