/**
 * @pension/ui — shared UI primitives for the workspace.
 *
 * Skeleton package. Intended to hold cross-app presentational components and
 * design tokens at the workspace level, mirroring what the app currently
 * keeps in `src/shared/components/ui`. Exports are minimal until components
 * are extracted.
 */

/** Canonical npm package name for the UI package. */
export const UI_PACKAGE_NAME = '@pension/ui';
