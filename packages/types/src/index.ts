/**
 * @pension/types — shared TypeScript type definitions for the workspace.
 *
 * Skeleton package. Intended to hold cross-app type contracts (entities,
 * DTOs, shared state shapes) shared between apps and packages. Exports are
 * minimal until real contracts migrate here.
 */

/** Canonical npm package name for the types package. */
export const TYPES_PACKAGE_NAME = '@pension/types';

/** Example shared type; replaced as real contracts migrate to this package. */
export type Test = {
  id: string;
};
