/**
 * @pension/lib — shared libraries and utilities for the workspace.
 *
 * Skeleton package. Intended to hold cross-app libraries (encryption,
 * http client, storage wrappers) that are too heavy for @pension/utils.
 * Exports are minimal until libraries are extracted from the app.
 */

/** Canonical npm package name for the lib package. */
export const LIB_PACKAGE_NAME = '@pension/lib';
