/**
 * @pension/utils — shared pure helpers for the workspace.
 *
 * Skeleton package. Intended to hold dependency-free utility functions
 * (formatting, parsing, guards) shared between apps. Exports are minimal
 * until helpers are extracted from the app.
 */

export * from './helpers';
