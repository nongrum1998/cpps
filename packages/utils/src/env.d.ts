/**
 * Ambient global declarations for `@pension/utils`.
 *
 * The package is framework-agnostic and must not depend on Expo or
 * React Native types, so the runtime globals it reads are declared here.
 */

/** True when running inside an Expo/React Native development build. */
declare const __DEV__: boolean;
