{
  "name": "pension-workspace",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter cssp-mobile dev",
    "export": "pnpm --filter cssp-mobile export",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "test": "pnpm -r test"
  },
  "dependencies": {
    "typescript": "6.0.3"
  }
}